{
    "name": "amplify_app", //"Existing Docker Compose (Extend)",
    "dockerComposeFile": [ // docker-compose.ymlのパスを記載
        "../docker-compose.yml"
    ],
    "service": "amplify", // docker-compose.yml に記載しているサービス名で起動したいコンテナ
    "workspaceFolder": "/usr/src" // 作業するパス "/workspace"
}